# This project now ships the live Alertmanager configuration under vps/alertmanager.yml.
# Keep this file around only if you need to experiment locally; copy the VPS config
# and point it at a throwaway webhook instead of committing secrets here.
