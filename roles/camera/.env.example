## Camera role env
# If you add camera services that need config/secrets, set them here
# and encrypt to .env.sops.enc using:  sops --encrypt --in-place .env

# Example non-secret var consumed by compose (has a default)
CAMERA_EXAMPLE_VAR=hello

