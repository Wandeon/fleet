# Playback device
AUDIO_OUTPUT_DEVICE=plughw:0,0
AUDIO_VOLUME=1.0

# Choose ONE of the following ways to define the stream:

# 1) Full URL (preferred for simplicity)
#STREAM_URL=http://stream.example.com:8000/pi-audio-01.opus

# 2) URL parts (the role builds STREAM_URL from these)
# For local icecast: use ICECAST_HOST=icecast (container name)
# For external icecast: use ICECAST_HOST=stream.example.com
ICECAST_SCHEME=http
ICECAST_HOST=icecast
ICECAST_PORT=8000
ICECAST_MOUNT=pi-audio-01.opus

# Icecast server passwords (only needed if running local icecast)
ICECAST_SOURCE_PASSWORD=hackme
ICECAST_ADMIN_PASSWORD=hackmeadmin

# Control API token (optional, recommended)
# Set the same value here as bearer for clients calling :8081
AUDIO_CONTROL_TOKEN=changeme-token

# Optional hardware mixer (amixer)
# Card index and control name depend on your HAT/sound card
AUDIO_MIXER_CARD=0
AUDIO_MIXER_CONTROL=Master
