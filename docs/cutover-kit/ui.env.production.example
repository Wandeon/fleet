# Fleet UI Production Environment Configuration
# Copy to .env and fill in actual values
#
# Token Sources:
# - <SAME-AS-API>: Must match the API_BEARER token from api/.env exactly

# =====================================================
# API INTEGRATION
# =====================================================

# API Configuration
PUBLIC_API_BASE=/api
PUBLIC_API_BEARER=<SAME-AS-API-BEARER-TOKEN>
# CRITICAL: This must exactly match API_BEARER from api/.env
# Source: Copy from the generated API_BEARER token (32 chars)
# Example: 1a2b3c4d5e6f7g8h9i0j1k2l3m4n5o6p7q8r9s0t1u2v3w4x5y6z

# =====================================================
# SERVER CONFIGURATION
# =====================================================

# UI Server (Docker container configuration)
PORT=3006
HOST=0.0.0.0

# Build Configuration
NODE_ENV=production

# =====================================================
# SETUP INSTRUCTIONS
# =====================================================
#
# 1. First, generate API_BEARER in api/.env:
#    cd ../api && grep API_BEARER .env
#
# 2. Copy that exact value to PUBLIC_API_BEARER above
#
# 3. Both API and UI must use the same bearer token for authentication
#
# =====================================================