# Fleet UI Production Environment Configuration
# Usually UI reads relative /api via reverse proxy; keep empty unless needed.

PUBLIC_API_BASE=/api
PUBLIC_API_BEARER=e921328089c426ad4c2112d0fd5fdd8705f9d72fed8ab4820424f8455cfea5e5

# Optional: Override API URL if not proxied
# PUBLIC_API_URL=http://fleet-api:3005